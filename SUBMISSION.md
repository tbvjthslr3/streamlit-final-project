# 제출용 설명글 (Slack DM 붙여넣기용)

아래 `---` 사이 내용을 그대로 복사해 곽경일 강사님 DM으로 보내시면 됩니다.

⚠️ **보내기 전 필수 확인**: 현재 배포 링크는 시크릿 창에서 로그인 벽이 뜹니다(비공개 앱).
아래 "제출 전 해결 필요" 항목을 먼저 처리해 주세요.

---

**[최종 개인 프로젝트 제출] 산업 리포트 Q&A 챗봇**

**배포 링크:** https://report-qna.streamlit.app/

## 1. 프로젝트 주제 및 기획 의도

**해결하려는 실무 문제** — 산업 리서치에서 수치 하나를 확인하려면 증권사·연구기관 보고서
수백 페이지를 매번 열어 Ctrl+F로 뒤져야 합니다. 보고서마다 수치와 기준연도가 달라 어느 자료의
숫자인지 다시 대조해야 하고, 그 대조 작업이 리서치 시간의 상당 부분을 잡아먹습니다.
"근거가 어느 보고서 몇 페이지인지" 다시 찾는 비용도 매번 발생합니다.

이 앱은 산업보고서 PDF 묶음을 하나의 지식베이스로 만들어, **질문 한 줄로 출처 보고서명과
페이지 번호까지 함께** 답을 돌려줍니다.

특정 산업 전용 앱이 아닙니다. 다루는 산업을 코드가 아니라 **지식베이스 정의 파일
(`data/sources.json`)** 이 결정하도록 설계해서, PDF와 정의 파일만 교체하면 반도체·2차전지·조선 등
어느 산업에서도 코드 수정 없이 재사용됩니다. 답변 프롬프트의 담당 영역("당신은 ○○ 산업을 담당하는
애널리스트입니다")과 화면의 예시 질문도 그 파일에서 자동 생성됩니다.

현재 배포본에는 시연용으로 **데이터센터 산업 공개 보고서 6종**(약 700페이지, 2,000개 검색 단위)이
미리 색인되어 있습니다 — 하나증권 데이터센터 In-depth(2026.07), 에너지경제연구원 기본연구 25-09,
에너지경제연구원 세계 에너지시장 인사이트 25-8호, IEA Energy and AI(2025.04),
Cushman & Wakefield 데이터센터 전력 과제(2025), 삼성증권 데이터센터 리포트(2024.04).
전부 발행기관이 웹에 무료 공개한 자료이며, 기밀·개인정보는 포함하지 않습니다.

## 2. 주요 기능 및 활용 방법

**주요 기능**

- **근거 인용 답변** — 답변 문장마다 [1][2] 근거 번호를 달고, 실제 발췌 원문·페이지 번호·원문 링크를 함께 표시
- **하이브리드 검색** — 임베딩 의미 검색과 BM25 키워드 검색을 RRF로 결합해, 의미 검색이 약한
  고유명사·수치·약어(예: "PUE", "B300") 질문까지 잡아냄
- **환각 억제** — 검색된 자료만 근거로 쓰도록 강제하고, 자료에 없으면 "제공된 보고서에서
  확인되지 않습니다"로 응답. 보고서 간 수치가 다르면 차이를 그대로 노출
- **문서 필터** — 특정 보고서만 골라 검색 범위 한정 (예: 국내 자료만, IEA만)
- **내 보고서 업로드** — PDF를 올리면 즉시 색인되어 기본 지식베이스와 **함께** 검색.
  공개 보고서와 사내 자료의 수치를 한 화면에서 대조 가능 (서버에 저장하지 않는 세션 한정 인덱스)
- **멀티턴 대화 + 스트리밍 응답** — "그럼 국내는?" 같은 후속 질문 처리, 응답 시간·사용 모델 표시

**활용 방법**

1. 사이드바에서 검색 대상 보고서를 선택합니다(기본: 전체).
2. 첫 화면의 예시 질문을 클릭하거나, 하단 입력창에 직접 질문합니다.
3. 답변 아래 **📎 근거 N건 보기**를 열어 실제 발췌문과 페이지를 확인합니다.
4. 다른 산업이나 사내 보고서를 다루려면 사이드바에서 PDF를 업로드해 색인합니다.

## 3. 사용된 핵심 기술 / 프레임워크

- **UI·배포**: Streamlit (`st.chat_message`, `st.write_stream`, `st.cache_resource`) + Streamlit Community Cloud
- **LLM**: OpenAI `gpt-4o-mini` (스트리밍 응답), 사이드바에서 `gpt-4.1-mini`/`gpt-4o` 전환 가능
- **임베딩**: OpenAI `text-embedding-3-small` (1536차원, L2 정규화)
- **검색**: NumPy 내적 기반 코사인 유사도 + `rank_bm25` + Reciprocal Rank Fusion(k=60)
- **문서 처리**: `pypdf` 페이지 단위 추출 → 문장 경계 인지 청킹(900자 / 150자 오버랩)

**설계상의 선택** — FAISS·ChromaDB·konlpy를 의도적으로 배제했습니다. 청크 2,000개 규모에서는
NumPy 내적으로 충분히 빠르고, 네이티브 빌드나 JVM 의존성이 없어야 Streamlit Cloud 배포가
안정적이기 때문입니다. 한국어 형태소 분석기 대신 어절 + 음절 bigram 토크나이저로 BM25를 구성했습니다.
벡터 인덱스는 배포 전에 미리 만들어 커밋하므로, 배포 환경에서 재임베딩이 발생하지 않아
콜드 스타트가 빠르고 접속자마다 임베딩 비용이 들지 않습니다.

**보안 처리** — OpenAI API 키는 Streamlit Secrets에만 저장하며 코드·화면·저장소 어디에도
노출되지 않습니다(`.env`, `secrets.toml`은 `.gitignore` 처리). 지식베이스는 전부 공개 자료이고,
업로드 문서는 서버에 저장되지 않습니다. 공개 배포용 가드레일로 세션당 질문 30회, 업로드 20MB,
질문 500자 제한을 두었습니다.

---

## 제출 전 해결 필요 (중요)

로그인하지 않은 상태로 https://report-qna.streamlit.app/ 에 접속하면 앱이 뜨지 않고
**"You do not have access to this app or it does not exist"** 로그인 벽이 나타납니다.
평가자가 시크릿 창으로 접속하면 이 화면을 보게 되므로, 과제 요건("제3자가 접속해도 정상 동작")을
충족하지 못합니다.

**원인**: private 저장소로 배포된 Streamlit 앱은 앱 자체도 private이 되어 초대받은 사람만 볼 수 있습니다.

**해결**: 저장소(`tbvjthslr3/streamlit-final-project`)를 public으로 전환하면 앱이 그대로 public이 됩니다.
재배포도, 새 URL도 필요 없습니다. 전환 시 증권사·연구기관 PDF는 저장소에서 제거하고
커밋 히스토리도 정리해 재배포를 피합니다(앱 동작에는 영향 없음 — 런타임에는 `data/index/`만 사용).

## 배포 후 최종 점검 체크리스트

- [ ] 시크릿 창(또는 다른 기기)으로 배포 URL 접속 → 로그인 벽 없이 앱이 뜨는지
- [ ] 사이드바에 "보고서 6건 · 검색 단위 2,000개"가 표시되는지
- [ ] API 키 입력창이 뜨지 않는지 (뜬다면 Secrets 설정 누락)
- [ ] 예시 질문 1건 실행 → 답변 + 근거 표시 확인
- [ ] '📎 근거 보기'에서 페이지 번호와 원문 링크 확인
- [ ] 임의의 PDF 1건 업로드 → 색인 후 질문 동작 확인
