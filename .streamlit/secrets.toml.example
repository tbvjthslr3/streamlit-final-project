# Streamlit Secrets 예시 파일.
# 로컬에서 쓰려면 이 파일을 secrets.toml 로 복사한 뒤 값을 채우세요.
# 배포 시에는 Streamlit Cloud > App settings > Secrets 에 같은 내용을 붙여넣습니다.
# 실제 secrets.toml 은 .gitignore 에 등록되어 있어 저장소에 올라가지 않습니다.

OPENAI_API_KEY = "sk-여기에-본인-키"
